apiVersion: apps/v1
kind: Deployment
metadata:
  name: python
  namespace: python
  labels:
    app: python
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
        - name: videogameapi
          image: videogameapi
          resources:
            requests:
                memory: "64Mi"
                cpu: "250m"
            limits:
                memory: "128Mi"
                cpu: "500m"
          ports:
            - name: http
              containerPort: 5050